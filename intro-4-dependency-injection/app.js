// Hey, it's app.js again. Purty nifty.
// We'll use this file the same way we did our old app.js
// That is, we'll use this to declare our application's namespace.
//
// Why don't we stop calling it 'intro' and start calling it 'app'?
var app = angular.module('app', []);
